# Server Configuration
PORT=4005
NODE_ENV=development
SERVICE_NAME=budget

# Database
BUDGET_REQUEST_DATABASE_URL=postgresql://postgres:nerieannganda247@localhost:5432/budget_db?schema=public

# ────────────────────────────────────────────────────────────────
# 🚀 Redis Cache Configuration (Upstash)
# ────────────────────────────────────────────────────────────────

# Enable/disable caching (set to 'true' to enable)
ENABLE_CACHE=true

# Upstash Redis TCP connection (primary - for ioredis)
REDIS_URL=

# Upstash REST API (fallback - for serverless environments)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Cache TTL (Time To Live) in seconds
CACHE_TTL_SECONDS=300

# Legacy Redis Configuration (for backward compatibility)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# JWT Authentication
JWT_SECRET=
JWT_DISABLED=true  # Set to false for production

# External Services
FINANCE_API_URL=http://localhost:4001
FINANCE_API_KEY=FINANCE_SERVICE_KEY

AUDIT_LOGS_API_URL=http://localhost:4004
AUDIT_API_KEY=FINANCE_DEFAULT_KEY

# Webhook Configuration
WEBHOOK_SECRET=your_webhook_signing_secret

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:3001

# Email Configuration (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Frontend URL
FRONTEND_URL=http://localhost:3001
